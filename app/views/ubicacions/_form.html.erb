<%= form_for(@ubicacion, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @ubicacion.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Cerrar</span></button>
      <h4><%= pluralize(@ubicacion.errors.count, "error") %> prohibited this ubicacion from being saved:</h4>

      <ul>
      <% @ubicacion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :latitud, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :latitud, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :longitud, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :longitud, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
