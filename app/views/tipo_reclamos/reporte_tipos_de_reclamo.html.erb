
<div class="page-header">
  <h4>Listado de  Tipos de Reclamos</h4>
</div>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
        <th>Nombre</th>
      </tr>
    </thead>

    <tbody>
      <% @tipo_reclamos.each do |tipo_reclamo| %>
        <tr>
          <td><%= tipo_reclamo.nombre %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
